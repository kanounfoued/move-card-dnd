import React from 'react';

const style = {
  root: {
    height: '100vh',
    width: '100vw',
    overflow: 'hidden',
  },
};

const App = () => {
  return <div style={style.root}>new App</div>;
};

export default App;
